{% extends 'base.html.twig' %}

{% block title %}Hello WishController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>



<div class="container">

    <div class="row">
        <div class="col">
            <h3 class="mt-5 text-center">Buckets</h3>
        </div>
    </div>

    {% for idee in ideeList %}
        <div class="card my-4">
            <div class="card-body">
                <h5 class="card-title">{{ idee.getTitle() }}</h5>
                <p class="card-text"><a class="fst-italic blockquote-footer" style="text-decoration: none;">{{ idee.getAuthor() }}</a></p>
                <a href="{{ path('app_detail', {id: idee.id}) }}" class="btn btn-primary" style="text-decoration: none;">Détail</a>
            </div>
        </div>

    {% endfor %}

</div>
{% endblock %}



{#<ul>#}
{#<li>{{ idee.getTitle() }} <a class="fst-italic blockquote-footer" style="text-decoration: none;">({{ idee.getAuthor() }})</a> <a href="{{ path('app_detail', {id: idee.id}) }}" style="text-decoration: none;">detail</a></li>#}
{#</ul>#}
